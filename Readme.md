文档支持：

[告别 AI 代码乱炖！GitHub 爆火中文 Vibe Coding 指南，Java 开发者的 AI 编程终极工作流-阿里云开发者社区](https://developer.aliyun.com/article/1720705#comment)

# 引言
此文件用于不断学习`Prompt`提示词工程。

所有文件要留痕，放入项目`.Doc`包下。

提示词工程始终处于不间断的动态更新中，我们需要根据开发中出现的问题，不断去优化提示词，把`AI`始终限制在我们给的框架中。提示词需要精细化，大到这个项目使用什么语言、技术架构，小到这一小模块需要实现什么具体功能，在不同开发阶段，对于提示词的样式也有不同的写法。

# 3大阶段的提示词规范
### 一切开始前
我们需要定义一份“宪法”，就命名为`项目计划书.md`，此文件为`AI`的规则文件，保证项目的大体质量

```cpp
# Java Spring Boot项目开发规范
## 角色定位
你是一名Java后端开发工程师，精通Spring Boot 3.x、MyBatis-Plus、Spring Security等主流框架，严格遵循阿里巴巴Java开发规范，生成高质量、可维护、高性能的Java代码。

## 编码规范
1. 严格遵循Java语言规范，使用驼峰命名法，类名使用大驼峰，方法名、变量名使用小驼峰，常量使用全大写下划线分隔；
2. 所有类、公共方法必须添加完整的Javadoc注释，说明功能、参数、返回值、异常、作者信息；
3. 方法单行长度不超过120个字符，代码必须格式化，缩进使用4个空格；
4. 必须使用Lombok简化样板代码，避免手动编写getter/setter、构造器等代码；
5. 所有异常必须妥善处理，禁止捕获异常后不做任何处理，必须打印日志或向上抛出；
6. 所有接口必须遵循RESTful规范，使用正确的HTTP方法（GET/POST/PUT/DELETE），返回统一的响应格式。
    
## 技术栈约束
- JDK版本：JDK 17 LTS
- Spring Boot版本：3.4.4
- 持久层框架：MyBatis-Plus 3.5.7
- 构建工具：Maven
- 日志框架：SLF4J + Logback
- 禁止使用已废弃的API，所有依赖必须使用稳定的LTS版本
    
## 代码生成要求
1. 生成代码前，必须先阅读项目现有结构、pom.xml依赖、已有的编码规范，确保生成的代码与现有项目完全兼容；
2. 生成的代码必须模块化，遵循单一职责原则，一个类只负责一个功能，禁止生成超大类、超大方法；
3. 所有业务代码必须包含完整的异常处理、参数校验，边界条件必须覆盖；
4. 生成功能代码时，必须同步生成对应的单元测试代码，使用JUnit 5 + Mockito框架；
5. 禁止生成硬编码内容，所有配置项必须放在application.yml配置文件中，通过@Value注解或配置类注入。

## 输出要求
1. 生成代码前，先说明本次修改涉及的文件、核心设计思路，确认后再输出代码；
2. 代码输出必须完整，包含完整的包名、导入语句、类定义、方法实现，禁止只输出代码片段；
3. 复杂逻辑必须添加行内注释，说明设计思路与实现逻辑；
4. 涉及多文件修改时，必须分文件输出，明确标注文件路径。
```

这个是定义在项目文件夹中的文件，下面我们来写在ChatGPT中，你需要给出的`Prompt`(举例)

> 我即将参加浙江省软件服务外包大赛，我要做到的项目名称是“金融企业智能办公提效平台“，参赛的原稿在PDFA21中，接下来的所有生成代码必须符合《项目计划书》中的要求，在开始写之前务必先阅读此文！我目前打算构建首页（展示基本基本信息）、会议（会议预约、纪要整理、事项总结）、企业知识库（分类放入文档）、AI模型（智能写作、PPT）、日历（展示待办信息，起到提示作用）、待办（同步日历）这几个模块，在上一个对话中，我和你已经完成了首页功能的设计，就在我给你的这个文件夹中，现在我们开始完成企业知识库的功能，其他的先不做，接下来我给你企业知识库的prompt，先完成以下工作  
输出项目的完整分层架构设计，模块拆分说明；
>
> 1. 输出项目的完整目录结构，明确每个文件夹的职责；
> 2. 输出MySQL建表语句，包含字段、注释、索引；
> 3. 输出项目的核心接口清单，包含HTTP方法、接口路径、请求参数、响应格式；
> 4. 输出项目的整体实施步骤。  
你有哪里不懂的直接问我，不要自己费时费力去瞎想
>

